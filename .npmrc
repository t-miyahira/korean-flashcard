# サプライチェーン攻撃対策

# postinstall 等のライフサイクルスクリプトを無効化
ignore-scripts=true

# 厳格な依存解決（依存の依存が勝手にバージョン上がるのを防止）
strict-peer-dependencies=true

# lockfile がない/古い場合にインストールを失敗させる（CI以外でも安全）
prefer-frozen-lockfile=true

# パッケージの完全性チェックを有効化
resolution-mode=highest
